"""A demonstration a room full of people can operate, one seat each.

The argument this project makes is about *who knows what*, and a single screen
showing everything is the one presentation that cannot make it. So the demo is
seats: a browser holds one role, sees exactly what that role would see, and is
given no way to see the rest. The taker sees its own order and its own price.
A maker sees its own policy and whether it won. A computing node sees shares,
which look like noise, and still helps produce the right answer.

Every seat may be held by a person or left to run itself, chosen per seat, so
one laptop with everything automatic and a room with nine laptops are the same
program with different seats claimed.
"""
